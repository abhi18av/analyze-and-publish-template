_subdirectory: template

# Basic Project Information
project_name:
  type: str
  help: What is your project name?
  validator: >-
    {% if not project_name %}
    Project name is required
    {% endif %}

short_name:
  type: str
  help: What is your project's short name? (used for file naming)
  validator: >-
    {% if not short_name %}
    Short name is required
    {% elif ' ' in short_name %}
    Short name cannot contain spaces
    {% endif %}

description:
  type: str
  help: Brief description of the project
  default: A comprehensive data science analysis project

# Author Information
author_name:
  type: str
  help: Your name or team name
  default: "Your Name"

author_email:
  type: str
  help: Contact email for the project
  default: "your.email@example.com"

# Project Configuration
programming_language:
  type: str
  help: Primary programming language
  choices:
    - Python
    - R
    - Both
  default: Both

python_version:
  type: str
  help: Python version to use
  default: "3.11"
  when: "{{ programming_language in ['Python', 'Both'] }}"

# Analysis Components
include_eda_templates:
  type: bool
  help: Include exploratory data analysis templates
  default: true

include_statistical_analysis:
  type: bool
  help: Include statistical analysis templates
  default: true

include_ml_components:
  type: bool
  help: Include machine learning workflow templates
  default: true

# Data Management
data_versioning:
  type: bool
  help: Include DVC setup for data versioning
  default: true

data_validation:
  type: bool
  help: Include data validation frameworks (Great Expectations, Pandera)
  default: true

# Documentation and Reporting
documentation_format:
  type: str
  help: Primary documentation format
  choices:
    - Markdown
    - Jupyter Notebooks
    - Quarto
    - RMarkdown
  default: Quarto

include_publication_templates:
  type: bool
  help: Include academic publication templates
  default: true

# Experiment Tracking
experiment_tracking:
  type: str
  help: Experiment tracking platform
  choices:
    - None
    - MLflow
    - Weights & Biases
    - Neptune
  default: MLflow

# CI/CD and Automation
github_actions:
  type: bool
  help: Add GitHub Actions workflows
  default: true

pre_commit_hooks:
  type: bool
  help: Configure pre-commit hooks for code quality
  default: true

# Deployment Options
include_deployment_templates:
  type: bool
  help: Include deployment templates (Docker, FastAPI, etc.)
  default: true

module_name:
  type: str
  help: Python module name (auto-generated from short_name)
  default: "{{ short_name|lower|replace(' ', '_')|replace('-', '_') }}"
  when: false  # Auto-generated, not user input

# description:
#   type: str
#   help: Brief description of the project
#   default: A data analysis project

# # Author Information
# author_name:
#   type: str
#   help: Your name or team name

# author_email:
#   type: str
#   help: Contact email for the project
#   default: ""

# # Project Configuration
# programming_language:
#   type: str
#   help: Primary programming language
#   choices:
#     - Python
#     - R
#     - Both
#   default: Both

# # Analysis Components
# include_eda_templates:
#   type: bool
#   help: Include exploratory data analysis templates
#   default: true

# include_statistical_analysis:
#   type: bool
#   help: Include statistical analysis templates
#   default: true

# include_ml_components:
#   type: bool
#   help: Include machine learning workflow templates
#   default: true

# # # Specialized Components
# # specialized_domain:
# #   type: str
# #   help: Include specialized domain-specific tools
# #   choices:
# #     - None
# #     - Bioinformatics
# #     - Epidemiology
# #     - Geospatial
# #     - Time Series
# #     - Other
# #   default: None

# # # Data Management
# data_versioning:
#   type: bool
#   help: Include DVC setup for data versioning
#   default: true

# data_validation:
#   type: bool
#   help: Include data validation frameworks (Great Expectations)
#   default: true

# # Documentation and Reporting
# documentation_format:
#   type: str
#   help: Primary documentation format
#   choices:
#     - Markdown
#     - Jupyter Notebooks
#     - Quarto
#     - RMarkdown
#   default: Quarto

# include_publication_templates:
#   type: bool
#   help: Include academic publication templates
#   default: true

# # Experiment Tracking
# experiment_tracking:
#   type: str
#   help: Experiment tracking platform
#   choices:
#     - None
#     - MLflow
#     - Weights & Biases
#   default: None

# # CI/CD and Automation
# github_actions:
#   type: bool
#   help: Add GitHub Actions workflows
#   default: true

# pre_commit_hooks:
#   type: bool
#   help: Configure pre-commit hooks for code quality
#   default: true

# # Deployment Options
# include_deployment_templates:
#   type: bool
#   help: Include deployment templates (Docker, FastAPI, etc.)
#   default: true



# # Multipass VM Support
# use_multipass:
#   type: bool
#   help: Include Multipass VM configuration for isolated environments
#   default: true

# multipass_profile:
#   type: str
#   help: Type of Multipass VM profile to include
#   choices:
#     - minimal
#     - data-science
#     - gpu-optimized
#   default: data-science
#   when: "{{ use_multipass }}"

# multipass_memory:
#   type: str
#   help: Memory allocation for Multipass VM
#   default: "4G"
#   when: "{{ use_multipass }}"

# multipass_cpus:
#   type: str
#   help: CPU cores for Multipass VM
#   default: "2"
#   when: "{{ use_multipass }}"

# multipass_disk:
#   type: str
#   help: Disk size for Multipass VM
#   default: "20G"
#   when: "{{ use_multipass }}"

# ssh_public_key:
#   type: str
#   help: Your SSH public key for accessing VMs (optional)
#   default: ""
#   when: "{{ use_multipass }}"
