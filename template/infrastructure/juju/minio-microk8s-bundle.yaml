series: jammy
description: Deploy MinIO on local MicroK8s using Juju
applications:
  minio:
    charm: minio
    channel: stable
    scale: 1
    trust: true
    options:
      access-key: minioadmin
      secret-key: minioadmin
    kubernetes:
      storage: microk8s-hostpath
machines: {}
